# Processed Data

This folder is reserved for generated CSV files such as a cleaned or combined SECOM dataset.

`model_comparison_results.csv` is allowed by `.gitignore` because it is useful report evidence after running the Colab training notebook. Other generated processed-data files are ignored by default.
