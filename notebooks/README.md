# Notebooks

Use `SECOM_Model_Training_Colab.ipynb` in Google Colab to train and compare the machine learning models. Restart the Colab session and run all cells so displayed results match the upgraded F1/recall-priority workflow. The notebook exports `models/secom_fault_detection_model.joblib`, which the Streamlit Cloud app loads for prediction and decision support.
